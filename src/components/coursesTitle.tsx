


const CoursesTitle = (props: {title: string}) => {
    return(
        <div>
            <h2 className="text-2xl"
            >{props.title}</h2>
        </div>
    )
}

export default CoursesTitle
